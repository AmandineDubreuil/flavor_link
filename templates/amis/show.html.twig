{% extends 'base.html.twig' %}

{% block title %}Amis
{% endblock %}

{% block body %}
	<h1>{{ ami.nom }}</h1>

	<div>
		Nombre de personnes :
		{{ ami.nbPersonnes }}
	</div>
	<div>
		<div>
			<a href="{{ path('app_amis_index') }}">Retour</a>
		</div>
		<div>
			<a href="{{ path('app_amis_edit', {'id': ami.id}) }}">Modifier</a>
		</div>
		{{ include('amis/_delete_form.html.twig') }}
	</div>

	<div>
		<h3>Recettes</h3>
		{% for recette in recettes %}
			<div>
				{{ recette.titre }}
			</div>
		{% else %}
			<div>
				Aucune recette enregistrée !
			</div>
		{% endfor %}
	</div>


{% endblock %}
