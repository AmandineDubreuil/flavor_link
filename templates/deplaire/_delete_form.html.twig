<form method="post" action="{{ path('app_deplaire_delete', {'id': deplaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deplaire.id) }}">
    <button class="btn">Delete</button>
</form>
