{% extends 'base.html.twig' %}

{% block title %}Edit Deplaire{% endblock %}

{% block body %}
    <h1>Edit Deplaire</h1>

    {{ include('deplaire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_deplaire_index') }}">back to list</a>

    {{ include('deplaire/_delete_form.html.twig') }}
{% endblock %}
