{% extends 'base.html.twig' %}

{% block title %}Deplaire{% endblock %}

{% block body %}
    <h1>Deplaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deplaire.id }}</td>
            </tr>
            <tr>
                <th>Ingredient</th>
                <td>{{ deplaire.ingredient }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_deplaire_index') }}">back to list</a>

    <a href="{{ path('app_deplaire_edit', {'id': deplaire.id}) }}">edit</a>

    {{ include('deplaire/_delete_form.html.twig') }}
{% endblock %}
