<form method="post" action="{{ path('app_detestes_delete', {'id': detestis.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detestis.id) }}">
    <button class="btn">Delete</button>
</form>
