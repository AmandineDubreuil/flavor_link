{% extends 'base.html.twig' %}

{% block title %}Detestes index{% endblock %}

{% block body %}
    <h1>Detestes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ingredient</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for deteste in detestes %}
            <tr>
                <td>{{ deteste.id }}</td>
                <td>{{ deteste.ingredient }}</td>
                <td>
                    <a href="{{ path('app_detestes_show', {'id': deteste.id}) }}">show</a>
                    <a href="{{ path('app_detestes_edit', {'id': deteste.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_detestes_new') }}">Create new</a>
{% endblock %}
