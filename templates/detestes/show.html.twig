{% extends 'base.html.twig' %}

{% block title %}Detestes{% endblock %}

{% block body %}
    <h1>Detestes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detestis.id }}</td>
            </tr>
            <tr>
                <th>Ingredient</th>
                <td>{{ detestis.ingredient }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_detestes_index') }}">back to list</a>

    <a href="{{ path('app_detestes_edit', {'id': detestis.id}) }}">edit</a>

    {{ include('detestes/_delete_form.html.twig') }}
{% endblock %}
