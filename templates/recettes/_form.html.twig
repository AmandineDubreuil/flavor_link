{{ form_start(form) }}

<div class="recettesTitre">
{{form_row(form.titre)}}
{{form_row(form.saison)}}
{{form_row(form.nbPersonnes)}}
</div>
<div class="ingredients">
<h4>ingredients :</h4>
	{{form_row(form.ingredientsAll)}}
{# <a href="{{ path('app_recette_ingredients_new') }}" class="btn">Ajouter</a> #}

</div>

<div class="recettesTemps">
	{{form_row(form.tpsPreparation)}}
	{{form_row(form.tpsCuisson)}}
	{{form_row(form.tpsRepos)}}
</div>

<div class="recettesPreparation">
{{form_row(form.preparation)}}
</div>

{{form_row(form.photo)}}

<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
